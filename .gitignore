__pycache__/
*.pyc
.DS_Store

# Local runtime artifacts -- regenerated by export_pipeline.py / main.py,
# don't belong in version control (the model bundle is trained fresh via
# `python export_pipeline.py`, and the DB is per-deployment state)
artifacts/underwriting.db
artifacts/plots/*.png

# Environment / secrets
.env
*.key
